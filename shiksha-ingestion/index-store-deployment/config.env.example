# Index Store VM Deployment Configuration - Example
# Copy this file to config.env and edit the values as needed

# ================================================================
# AZURE CONFIGURATION
# ================================================================

# Azure Resource Group - will be created if it doesn't exist
RESOURCE_GROUP="ShikshaMSRTest"

# Azure region where resources will be deployed
# Popular options: eastus, westus2, centralus, westeurope, southeastasia
LOCATION="eastus"

# ================================================================
# VIRTUAL MACHINE CONFIGURATION
# ================================================================

# Name of the VM to be created
VM_NAME="index-store-vm"

# VM Size - determines CPU and memory
# Standard_D4s_v3 = 4 vCPUs, 16GB RAM (recommended)
# Standard_D2s_v3 = 2 vCPUs, 8GB RAM (minimal)
# Standard_D8s_v3 = 8 vCPUs, 32GB RAM (high performance)
VM_SIZE="Standard_D2s_v3"

# Admin username for SSH access to the VM
ADMIN_USERNAME="shiksha-admin"

# ================================================================
# NEO4J CONFIGURATION
# ================================================================

# Password for Neo4j database
# Username will be 'neo4j', this sets the password
# Use a strong password with mix of letters, numbers, and symbols
NEO4J_PASSWORD="ShikshaSecure2025!"

# ================================================================
# NETWORK CONFIGURATION
# ================================================================
# These values are auto-generated based on VM_NAME
# Usually no need to change unless you have specific requirements

# Network Security Group name
NSG_NAME="${VM_NAME}-nsg"

# Virtual Network name
VNET_NAME="${VM_NAME}-vnet"

# Subnet name
SUBNET_NAME="${VM_NAME}-subnet"

# Public IP name
PUBLIC_IP_NAME="${VM_NAME}-pip"

# Network Interface name
NIC_NAME="${VM_NAME}-nic"

# ================================================================
# DATA DISK CONFIGURATION
# ================================================================

# Data disk name
DISK_NAME="${VM_NAME}-data-disk"

# Disk size in GB
DISK_SIZE_GB=128

# Logical Unit Number for the data disk
DATA_DISK_LUN=0

# Mount point for the data disk
DATA_MOUNT="/mnt/data"

# ================================================================
# QDRANT CONFIGURATION
# ================================================================

# Optional: set this if you want to restrict Qdrant access (comma-separated CIDRs)
# Example: "1.2.3.4/32,5.6.7.0/24"
QDRANT_ALLOWED_CIDRS="*"    # "*" means open to all on NSG rules (default)

# ================================================================
# EXAMPLE CONFIGURATIONS FOR DIFFERENT SCENARIOS
# ================================================================

# Development Environment:
# RESOURCE_GROUP="dev-shiksha-rg"
# VM_SIZE="Standard_D2s_v3"
# NEO4J_PASSWORD="DevPassword123!"

# Production Environment:
# RESOURCE_GROUP="prod-shiksha-rg"
# VM_SIZE="Standard_D8s_v3"
# NEO4J_PASSWORD="SuperSecureProductionPassword456!"

# Testing Environment:
# RESOURCE_GROUP="test-shiksha-rg"
# VM_SIZE="Standard_D4s_v3"
# NEO4J_PASSWORD="TestPassword789!"

# ================================================================
# DEPLOYMENT NOTES
# ================================================================
# 1. Make sure you have Azure CLI installed and logged in
# 2. Ensure SSH keys exist at ~/.ssh/id_rsa and ~/.ssh/id_rsa.pub
# 3. Choose a location close to your users for better performance
# 4. VM will have persistent data storage mounted at /mnt/data
# 5. Neo4j will be accessible via web interface on port 7474
# 6. Neo4j Bolt protocol will be available on port 7687
# 7. Qdrant vector database will be available on port 6333
# 8. Data persists even if Docker containers are restarted
